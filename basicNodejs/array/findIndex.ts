// Retorna o índice do primeiro elemento no array que satisfaz a função de teste fornecida
let arrFindIndex = [1, 2, 3, 4];
let foundIndex = arrFindIndex.findIndex(x => x > 2);
console.log(foundIndex); // 2
