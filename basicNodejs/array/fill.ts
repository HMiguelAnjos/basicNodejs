// Preenche todos os elementos do array do índice inicial ao índice final com um valor estático
// array.fill(value, start, end)
let arrFill = [1, 2, 3, 4];
arrFill.fill(0, 1, 3);
console.log(arrFill); // [1, 0, 0, 4]
